AI HANDOFF — Lost Kingdom
Дата: 2026-06-12
Ветка: feature/map-navigation-camera-fog
PR: https://github.com/ikulikov99-tech/Lost-Kingdom/pull/2

════════════════════════════════════════════════════════════════
ЧТО СДЕЛАНО ПОСЛЕДНИМ (Итерация 2 — аудит механики исследования)
════════════════════════════════════════════════════════════════

Аудит обобщённого road-click по 4 вопросам + 2 минимальных фикса в _input:

1. Слепой icon-click сквозь туман на неоткрытую Path2D-локацию
   заблокирован: иконка работает только если точка локации видима
   (_is_road_point_visible). ROAD_PATHS-маршруты сохраняют icon-click,
   иначе тупик (у них нет Path2D).
2. Посреди дороги — только клики по дороге: icon-fallback строил путь
   от ИСХОДНОЙ локации (телепорт-возврат героя через всю карту).

РЕЗУЛЬТАТЫ АУДИТА:
- Последовательное исследование только дорогами: ДА для 6 Path2D-дорог;
  3 ROAD_PATHS-маршрута проходятся через icon-click (намеренно)
- Новые дороги после раскрытия: ДА — arrival делает соседей available,
  их дороги сразу кликабельны (подтверждено тестом Village→Dock)
- Ранний доступ к скрытым локациям: устранён (фикс 1)
- Застревание: НЕ найдено — вперёд всегда можно дойти кликами в зоне
  видимости героя (160px > ROAD_CLICK_DIST); реверс из локации работает
  (подтверждено живой сессией Dock→Village off 562→410)

ПРОВЕРКА выполнена временным in-game selftest (УДАЛЁН перед коммитом):
  foreign/invisible road rejected: true
  blind icon click blocked: true
  backward click rejected: true
  forward inching → auto-arrival Village (2/9)
  village->dock road click accepted: true

════════════════════════════════════════════════════════════════
ИЗМЕНЁННЫЕ ФАЙЛЫ
════════════════════════════════════════════════════════════════

Main.gd — только _input: +14/-2 (два guard'а)

════════════════════════════════════════════════════════════════
ПРОВЕРКА
════════════════════════════════════════════════════════════════

- gdlint: 34 issue — ровно базовый уровень (все пред-существующие:
  mixed-tabs/class-order/max-returns в _try_move_to/_draw_roads)
- Запуск quick-start: ошибок нет
- Скриншот: .claude/iter2_test.png

ПРИМЕЧАНИЕ: computer-use отклонён пользователем — живой UI-тест
выполнен через временный selftest, вызывающий реальные функции
механики (_try_road_click и т.д.) с печатью результатов.

════════════════════════════════════════════════════════════════
ИЗВЕСТНЫЕ ОГРАНИЧЕНИЯ
════════════════════════════════════════════════════════════════

- Посреди дороги назад идти нельзя (только вперёд к dest) — осознанно
- Герой идёт задом на обратных маршрутах (flip_h, Hero.gd) — отложено
- 3 маршрута не на Path2D: EarthMageCastle↔DarkCastle, ↔Mine,
  Lumbermill↔Mine — там работает старый icon-click

════════════════════════════════════════════════════════════════
СЛЕДУЮЩИЙ ШАГ
════════════════════════════════════════════════════════════════

НЕ делать пока: события, засады, квесты, UI, облака.

Кандидаты:
1. Перевести 3 маршрута на Path2D → road-click покроет всю карту
2. Реверс посреди дороги (вернуться к origin без прибытия в dest)
3. flip_h героя (Hero.gd)

════════════════════════════════════════════════════════════════
КОНТЕКСТ ДЛЯ НОВОГО ИИ
════════════════════════════════════════════════════════════════

RPG карта мира, Godot 4.6. Исследование кликами по видимым участкам
Path2D-дорог; туман войны (гибрид: облака + маска) раскрывается по пути.

Запуск: quick-start = run_project с scene res://Main.tscn.
gdlint работает. Скриншоты: PrintWindow по ProcessName=Godot*.
Координаты — ТОЛЬКО из MAP_COORDINATES.md.
Полная архитектура: PROJECT_BACKUP.md
